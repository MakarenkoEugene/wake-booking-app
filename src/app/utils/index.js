export const replaceEmoji = (text) => text
  .replace('🙂', ':)')
  .replace('😞', ':(')
  .replace('😛', ':P')
  .replace('😃', ':D')
  .replace('😉', ';)')
  .replace('👍', '(y)')
  .replace('👎', '(n)')
  .replace('💡', '(on)');
